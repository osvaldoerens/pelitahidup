<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pelitahidup.gpib.pelitahidup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SbuActivity"></activity>
        <activity android:name=".WartaJemaatActivity" />
        <activity android:name=".InformasiActivity" />
        <activity android:name=".DataJemaatActivity" />
        <activity android:name=".JadwalIbadahActivity" />
        <activity android:name=".VisiMisiActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
    </application>

</manifest>